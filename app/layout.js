import "./globals.css";
export const metadata = { title:"NEXORA — Digital Marketplace", description:"NEXORA marketplace" };
export default function RootLayout({children}) {
  return <html lang="vi"><body>{children}</body></html>;
}