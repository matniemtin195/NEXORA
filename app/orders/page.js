"use client";
import{useEffect,useState}from"react";import{supabaseBrowser}from"../../lib/supabase";
const money=n=>new Intl.NumberFormat("vi-VN").format(Number(n||0))+"đ";const status={paid:"Đã thanh toán",processing:"Đang xử lý",completed:"Hoàn tất",cancelled:"Đã hủy"};
export default function Orders(){const sb=supabaseBrowser();const[orders,setOrders]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{sb.auth.getUser().then(async({data})=>{if(!data.user){location.href="/account";return}const{data:o}=await sb.from("orders").select("*,order_items(*)").eq("user_id",data.user.id).order("created_at",{ascending:false});setOrders(o||[]);setLoading(false)})},[]);return <><SimpleHeader/><main className="wrap page"><div className="title"><div><h1>Đơn hàng của tôi</h1><p>Lịch sử giao dịch và thông tin bàn giao</p></div><a className="btn mini" href="/products">Mua thêm</a></div>{loading?<div className="empty">Đang tải...</div>:orders.length===0?<div className="empty">Chưa có đơn hàng.<br/><a href="/products">Khám phá sản phẩm →</a></div>:orders.map(o=><article className="order" key={o.id}><div><b>#{o.id.slice(0,8)}</b><span>{new Date(o.created_at).toLocaleString("vi-VN")}</span>{o.order_items?.map(i=><small key={i.id}>{i.product_name} × {i.quantity}</small>)}</div><strong>{money(o.total)}</strong><label className="status">{status[o.status]||o.status}</label><details><summary>Thông tin bàn giao</summary><pre>{o.delivery_text||"Đơn hàng đang được xử lý. Nếu chưa nhận được sản phẩm, hãy liên hệ hỗ trợ."}</pre></details></article>)}</main></>}
function SimpleHeader(){return <header><a className="brand" href="/"><span>N</span><div><b>NEXORA</b><small>DIGITAL MARKETPLACE</small></div></a><nav><a href="/">Trang chủ</a><a href="/products">Sản phẩm</a><a href="/wallet">Ví</a><a href="/account">Tài khoản</a></nav></header>}
